name: CI
# Railway handles CD automatically on push to main

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v7

      - name: Set up JDK 25
        uses: actions/setup-java@v5
        with:
          java-version: '25'
          distribution: 'temurin'
          cache: 'maven'
          server-id: github
          server-username: GITHUB_ACTOR
          server-password: GITHUB_TOKEN

      - name: Build and test
        run: ./mvnw clean verify -U -Dgroups='!local'
        env:
          ENCRYPTION_KEY: 12345678901234567890123456789012
          GITHUB_TOKEN: ${{ secrets.PACKAGES_TOKEN }}